apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dynakube-chart.fullname" . }}-test-connection"
  labels:
    {{- include "dynakube-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dynakube-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
